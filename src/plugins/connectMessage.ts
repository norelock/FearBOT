import FearBot from "../client/client";
import { InstanceFeature } from "../utils/interfaces";

const connectMessage = async (bot: FearBot, plugin: InstanceFeature) => {};

export default connectMessage;